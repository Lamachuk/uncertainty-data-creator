#![allow(unused)]

// cargo run src/main.rs

use rand::Rng;
use std::fs::File;
use std::io;
use std::io::{BufRead, BufReader, ErrorKind, Write};

use std::thread;
use std::time::Duration;

use std::cell::RefCell;
use std::rc::Rc;
use std::sync::{Arc, Mutex};

/**
 * Main function
 */

fn main() {
    println!("Running the main RUST function.");
}